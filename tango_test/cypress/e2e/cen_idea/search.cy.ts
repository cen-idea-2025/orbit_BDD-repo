/// <reference types="cypress" />

describe("", () => {
  it("", () => {
    cy.loginID();

    cy.get('input[placeholder*= "Search with any keyword..."]')
      .type("master")
      .type("enter");
  });
});
