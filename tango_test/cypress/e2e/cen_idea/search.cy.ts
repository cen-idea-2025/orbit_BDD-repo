/// <reference types="cypress" />

describe("", () => {
  it("", () => {
    cy.get('input[placeholder*="Search courses with any keyword or any"]')
      .type("master")
      .type("enter");
  });
});
